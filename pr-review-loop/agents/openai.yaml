interface:
  display_name: "PR Review Loop"
  short_description: "Publish PRs and keep fixing review feedback"
  default_prompt: "Use $pr-review-loop to publish the current changes and monitor the PR review loop."
policy:
  allow_implicit_invocation: true
